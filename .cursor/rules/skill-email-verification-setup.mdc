---
description: "邮箱验证功能实现技能"
alwaysApply: true
---

> **Source of truth**: `.github/skills/email-verification-setup.md` (edit there, then re-run this script.)

# 邮箱验证功能实现技能

## 后端邮件配置（Spring Boot）

**QQ邮箱SMTP配置**：
```yaml
spring:
  mail:
    host: smtp.qq.com
    port: 587  # STARTTLS端口
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}  # QQ邮箱授权码
    properties:
      mail.smtp:
        auth: true
        starttls:
          enable: true
          required: true
        ssl.trust: smtp.qq.com

app:
  email:
    from: ${MAIL_USERNAME}
    frontend-base-url: ${FRONTEND_URL:http://localhost:5173}
    verification:
      verify-path: /verify-email
```

**关键点**：
1. 使用587端口+STARTTLS（比465端口更稳定）
2. 密码必须是QQ邮箱授权码（非QQ密码）
3. 必须配置`app.email.from`发件人地址

## 前端验证页面实现（React）

**1. 创建页面组件** `pages/VerifyEmail/VerifyEmail.tsx`：
```tsx
const [searchParams] = useSearchParams();
const [status, setStatus] = useState<'verifying' | 'success' | 'error'>('verifying');

useEffect(() => {
  const token = searchParams.get('token');
  if (!token) {
    setStatus('error');
    return;
  }
  
  authApi.verifyEmail(token)
    .then(() => setStatus('success'))
    .catch(() => setStatus('error'));
}, [searchParams]);
```

**2. 添加路由** `App.tsx`：
```tsx
<Route path="/verify-email" element={<VerifyEmail />} />
```

**3. 页面显示三态**：
- 验证中：加载动画
- 成功：绿色✓ + "立即登录"按钮
- 失败：红色✕ + 错误信息 + "返回登录"按钮

## 后端API实现

```java
@PostMapping("/verify-email")
public ResponseEntity<ApiResponse<Void>> verifyEmail(
        @RequestParam @NotBlank String token) {
    authService.verifyEmail(token);
    return ResponseEntity.ok(ApiResponse.ok("邮箱验证成功"));
}
```

## 测试流程

1. 注册用户 → 发送验证邮件
2. 用户点击邮件链接 `{frontend-url}/verify-email?token={token}`
3. 前端自动提取token调用后端API
4. 显示验证结果 → 跳转登录

## 常见问题

**问题1**：无法连接SMTP服务器
- 解决：使用587端口替代465端口

**问题2**：验证链接点击无响应
- 原因：前端缺少`/verify-email`路由
- 解决：创建页面组件并配置路由

**问题3**：邮件发送失败但无错误
- 检查：`app.email.from`是否配置
- 检查：授权码是否正确（16位）

## 快速复用清单

后端：
- [ ] 配置SMTP（application.yml）
- [ ] 设置环境变量（MAIL_USERNAME, MAIL_PASSWORD）
- [ ] 实现EmailService发送邮件
- [ ] 实现`POST /verify-email`接口

前端：
- [ ] 创建VerifyEmail页面组件
- [ ] 添加/verify-email路由
- [ ] 实现三态显示逻辑
- [ ] 添加authApi.verifyEmail方法

## 参考文档

详细配置见：`docs/EMAIL_SERVER_SETUP.md`
测试指南见：`docs/EMAIL_VERIFICATION_TEST.md`
