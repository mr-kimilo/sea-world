---
description: "Use when writing React components, TypeScript hooks, Zustand stores, Axios API calls, or routing code in the frontend."
globs: frontend/**/*
alwaysApply: false
---

# Frontend Conventions (React + TypeScript)

## 项目结构

```
src/
├── api/          # Axios 封装，每个域一个文件（auth.ts, score.ts, shop.ts）
├── store/        # Zustand store，按域拆分
├── hooks/        # 自定义 Hooks（use 开头）
├── pages/        # 页面组件，每个页面独立文件夹
├── components/   # 通用组件
├── types/        # TypeScript 类型定义（index.ts 统一导出）
└── utils/        # 纯函数工具
```

## TypeScript 规范

- 所有 props、API 响应、store state 必须定义类型，禁止 `any`
- API 响应使用 `ApiResponse<T>` 泛型包装
- 枚举值用 TypeScript `type` union 而非 `enum`：

```ts
// ✅
type ScoreCategory = 'intelligence' | 'physical' | 'moral' | 'hygiene' | 'handcraft';

// ❌
enum ScoreCategory { intelligence, physical, ... }
```

## 组件规范

- 函数组件 + TypeScript，禁止 class 组件
- Props 用 `interface` 定义，单独导出便于复用
- 组件文件与同名 CSS 文件放在同一文件夹

```tsx
// ✅
interface ChildCardProps {
  child: Child;
  onSelect: (id: string) => void;
}

export default function ChildCard({ child, onSelect }: ChildCardProps) { ... }
```

## 国际化 (i18n)

**强制要求**：所有用户可见的字符串必须通过 `react-i18next` 管理，禁止硬编码。

```tsx
// ❌ 禁止
<button>登录</button>
<input placeholder="请输入邮箱" />

// ✅ 必须
const { t } = useTranslation('auth');
<button>{t('login.submit')}</button>
<input placeholder={t('login.emailPlaceholder')} />
```

详细规范见 [frontend-i18n.instructions.md](frontend-i18n.instructions.md)。

## 状态管理 (Zustand)

- 每个业务域一个 store 文件：`authStore.ts`, `familyStore.ts`
- 使用 `persist` 中间件持久化登录状态
- store 内不执行 API 调用，由组件或 hook 调用后 `set` 更新

```ts
// ✅ store 只管理状态
const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      login: (user, tokens) => set({ user, ...tokens, isAuthenticated: true }),
      logout: () => set({ user: null, isAuthenticated: false }),
    }),
    { name: 'sea-world-auth' }
  )
);
```

## API 调用规范

- 所有请求通过 `src/api/client.ts` 的 Axios 实例发出（含 Token 注入拦截器）
- 每个域对应一个 API 文件，函数名与后端路径语义对应：

```ts
// src/api/score.ts
export const scoreApi = {
  create: (data: CreateScoreRequest) =>
    api.post<ApiResponse<ScoreRecord>>('/scores', data),
  list: (params: ScoreQueryParams) =>
    api.get<ApiResponse<PageResult<ScoreRecord>>>('/scores', { params }),
};
```

- 组件内错误处理：

```tsx
// ✅ 错误消息必须国际化
const { t } = useTranslation('validation');

try {
  await scoreApi.create(form);
} catch (err: unknown) {
  if (axios.isAxiosError(err)) {
    const errorCode = err.response?.data?.code;
    setError(t(`errors.${errorCode}`, t('errors.unknown')));
  }
}
```

## 路由与鉴权

- 需要登录的页面用 `<ProtectedRoute>` 包裹
- 管理员页面用 `<ProtectedRoute requiredRole="admin">` 包裹
- 未登录时自动重定向到 `/login`，登录后跳回原页面

## 样式约定

- 每个页面/组件有独立 `.css` 文件，避免全局样式污染
- 使用 CSS 变量定义主题色：`var(--color-primary)`
- 移动端优先，使用 flexbox/grid 布局
- **流式 / 响应式**：页面与组件须遵守 [frontend-fluid-layout.instructions.md](frontend-fluid-layout.instructions.md)（1024px 断点与 `useDeviceType` 一致，防横向溢出与裁切）
