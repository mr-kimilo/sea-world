---
description: "Use when writing Java classes, methods, fields, DTOs, or entities in the backend. Covers package naming, class/method/variable/constant naming, and DTO naming conventions."
globs: backend/**/*.java
alwaysApply: false
---

# Java Naming Conventions

## Packages
全小写，按层级划分：`com.seaworld.controller`, `com.seaworld.service`, `com.seaworld.repository`

## Classes & Interfaces
- PascalCase 名词：`AuthService`, `AuthController`, `UserRepository`
- Interface 不加 `I` 前缀；实现类仅在**有多个实现**时加 `Impl` 后缀
- Enum：PascalCase 类名，值与数据库保持一致（`lowercase` 或 `UPPER_SNAKE_CASE`）

## Methods
camelCase，动词开头：`findByEmail()`, `createFamily()`, `validateScore()`

## Variables & Fields
- camelCase：`familyId`, `availableScore`
- 布尔值加 `is/has/can` 前缀：`isActive`, `hasPermission`, `canModify`
- 常量：`UPPER_SNAKE_CASE`，例如 `MAX_DAILY_SCORE = 10`

## Entity Fields
camelCase 映射 snake_case 列名，必须显式声明：

```java
// ✅
@Column(name = "available_score")
private Integer availableScore;

// ❌ 依赖 JPA 自动推断转换
private Integer availableScore;
```

## DTOs
按用途命名，**禁止直接返回或接收 Entity**：

| 场景 | 命名 |
|------|------|
| 客户端请求体 | `XxxRequest` |
| 服务端响应体 | `XxxResponse` |
| 内部传输 | `XxxDTO` |
