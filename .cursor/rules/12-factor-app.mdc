---
description: "12-Factor App: codebase, config, logs, processes — whole project."
alwaysApply: true
---

# 12-Factor App 原则 - 项目实施指南

> 适用于：所有后端和前端代码、配置文件、部署脚本

本文档基于 [12-Factor App](https://12factor.net/) 方法论，定义了本项目的配置管理、部署和开发标准。Copilot 在生成代码、建议优化时应遵循这些原则。

---

## I. Codebase (代码库)
**原则**: 一个代码库，多次部署

### ✅ 项目实施
- **单一仓库**: `world-under-the-sea-2` 包含所有代码（后端 + 前端）
- **多环境部署**: dev / test / staging / prod 使用相同代码，不同配置
- **版本控制**: 所有代码提交到 Git

### 🔍 检查点
- [ ] 后端和前端在同一仓库
- [ ] 不同环境不应有代码分支（只有配置差异）
- [ ] `.gitignore` 正确配置，不提交编译产物和敏感信息

### ❌ 禁止做法
```java
// ❌ 不要在代码中判断环境
if (System.getenv("ENV").equals("prod")) {
    // 生产环境逻辑
}

// ✅ 应该通过配置注入
@Value("${app.feature.enabled}")
private boolean featureEnabled;
```

---

## II. Dependencies (依赖)
**原则**: 显式声明依赖关系

### ✅ 项目实施
- **后端**: `pom.xml` 声明所有 Maven 依赖
- **前端**: `package.json` 声明所有 npm 依赖
- **版本锁定**: 使用精确版本号（避免 `LATEST` 或 `*`）

### 🔍 检查点
- [ ] 所有依赖在 `pom.xml` 或 `package.json` 中显式声明
- [ ] 不依赖系统级包（如不假设系统已安装 ImageMagick）
- [ ] 依赖版本精确指定（避免范围版本如 `^1.0.0`）

### ✅ 示例
```xml
<!-- ✅ pom.xml 中精确指定版本 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
    <version>3.2.5</version>
</dependency>

<!-- ❌ 避免使用 LATEST -->
<version>LATEST</version>
```

```json
// ✅ package.json 锁定版本
{
  "dependencies": {
    "react": "18.2.0",
    "axios": "1.6.0"
  }
}
```

### 📦 依赖隔离
- **后端**: 使用 Maven 本地仓库 `~/.m2/repository`
- **前端**: 使用 `node_modules/`（已在 `.gitignore`）
- **Docker**: 在容器内安装依赖（未来实施）

---

## III. Config (配置)
**原则**: 在环境中存储配置 ⭐ **核心原则**

### ✅ 项目实施

#### 配置分层架构
```
application.yml               ← 通用配置（数据库连接池、日志级别）
application-dev.yml           ← 开发环境结构（占位符）
application-prod.yml          ← 生产环境结构（占位符）
application-local.yml         ← 本地覆盖（不提交 Git）
.env / 系统环境变量            ← 真实密码（不提交 Git）
```

#### 配置来源优先级
1. **系统环境变量** (最高优先级)
2. `.env` 文件
3. `application-{profile}.yml`
4. `application.yml`

### 🔍 检查点
- [ ] **所有敏感信息**使用环境变量（密码、密钥、Token）
- [ ] yml 文件中使用 `${VAR_NAME:default}` 占位符
- [ ] `.env`、`application-local.yml` 在 `.gitignore` 中
- [ ] 不同环境的配置差异仅限于环境变量值

### ✅ 正确做法
```yaml
# application-prod.yml ✅
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}  # 占位符，不是真实密码

app:
  jwt:
    secret: ${JWT_SECRET}  # 必须从环境变量读取
```

```bash
# .env 文件（不提交到 Git）✅
DB_URL=jdbc:postgresql://prod-db.example.com:5432/seaworld
DB_USERNAME=seaworld_prod
DB_PASSWORD=Str0ngP@ssw0rd!2024
JWT_SECRET=base64_encoded_random_secret_key
```

### ❌ 禁止做法
```yaml
# ❌ 不要在 yml 中硬编码敏感信息
spring:
  datasource:
    password: MyRealPassword123  # 会被提交到 Git！

app:
  jwt:
    secret: hardcoded_secret_key  # 极度危险！
```

### 🛠️ Copilot 行为规范
当 Copilot 生成配置相关代码时：
- ✅ 使用 `@Value("${property.name}")` 注入配置
- ✅ 建议使用 `@ConfigurationProperties` 管理复杂配置
- ❌ 不要生成硬编码的密码、密钥、API Token
- ❌ 不要建议把敏感信息放在 yml 文件中

---

## IV. Backing Services (后端服务)
**原则**: 把后端服务当作附加资源

### ✅ 项目实施
- **数据库**: PostgreSQL（通过 JDBC URL 连接）
- **邮件服务**: SMTP（通过配置切换不同邮件服务器）
- **缓存**: Redis（未来可能添加）

### 🔍 检查点
- [ ] 后端服务通过 URL 配置连接（数据库、邮件、缓存）
- [ ] 可以不修改代码切换服务提供商（如从本地 PostgreSQL 切换到 AWS RDS）
- [ ] 没有硬编码服务地址

### ✅ 示例
```yaml
# ✅ 可配置的后端服务
spring:
  datasource:
    url: ${DB_URL}  # 可切换到任何 PostgreSQL 实例
  mail:
    host: ${MAIL_HOST}  # 可切换 SMTP 服务器
```

```java
// ✅ 通过接口抽象后端服务
@Service
public class EmailService {
    @Autowired
    private JavaMailSender mailSender;  // 可替换实现
}

// ❌ 不要硬编码服务地址
// String dbHost = "192.168.1.100";  // 硬编码
```

---

## V. Build, Release, Run (构建、发布、运行)
**原则**: 严格分离构建和运行

### ✅ 项目实施

#### 构建阶段 (Build)
```bash
# 后端：生成 JAR 文件
mvn clean package -DskipTests

# 前端：生成静态文件
npm run build
```

#### 发布阶段 (Release)
```bash
# 构建产物 + 配置 = 发布版本
发布 = JAR 文件 + .env 文件 + Nginx 配置
```

#### 运行阶段 (Run)
```bash
# 运行发布版本
java -jar sea-world-1.0.0.jar --spring.profiles.active=prod
```

### 🔍 检查点
- [ ] 构建产物与环境无关（同一个 JAR 可部署到任何环境）
- [ ] 发布版本有唯一标识（Git tag + 时间戳）
- [ ] 不在运行时编译代码

### ❌ 禁止做法
```bash
# ❌ 不要在生产环境直接运行源码
mvn spring-boot:run  # 这是开发环境做法

# ✅ 应该运行构建好的 JAR
java -jar target/sea-world-1.0.0.jar
```

---

## VI. Processes (进程)
**原则**: 以一个或多个无状态进程运行应用

### ✅ 项目实施
- **无状态**: Spring Boot 应用不依赖进程内存存储会话
- **会话管理**: JWT 存储在客户端（无服务端会话）
- **数据持久化**: 使用 PostgreSQL 数据库

### 🔍 检查点
- [ ] 不使用服务器端 Session（使用 JWT）
- [ ] 不在内存中存储用户状态（除缓存）
- [ ] 应用可以水平扩展（多实例运行）

### ✅ 正确做法
```java
// ✅ 使用 JWT 无状态认证
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    // JWT 自包含用户信息，不依赖服务器状态
}

// ✅ 临时数据存储到数据库
@Entity
public class RefreshToken {
    private String token;  // 持久化到数据库
}
```

### ❌ 禁止做法
```java
// ❌ 不要使用内存中的静态变量存储用户状态
public class UserCache {
    private static Map<String, User> cache = new HashMap<>();  // 危险！
}

// ❌ 不要使用 HttpSession
@GetMapping("/profile")
public User getProfile(HttpSession session) {
    return (User) session.getAttribute("user");  // 不推荐
}
```

---

## VII. Port Binding (端口绑定)
**原则**: 通过端口绑定提供服务

### ✅ 项目实施
- **后端**: Spring Boot 内嵌 Tomcat，监听 `${SERVER_PORT:8080}`
- **前端开发服务器**: Vite Dev Server 监听 5173
- **Nginx**: 反向代理监听 80/443

### 🔍 检查点
- [ ] 应用自包含 Web 服务器（不需要外部 Apache/Tomcat）
- [ ] 端口通过环境变量配置
- [ ] 不依赖运行时注入 Web 服务器

### ✅ 示例
```yaml
# application.yml
server:
  port: ${SERVER_PORT:8080}  # 可配置端口
```

```bash
# 可以通过环境变量覆盖
SERVER_PORT=9090 java -jar app.jar
```

---

## VIII. Concurrency (并发)
**原则**: 通过进程模型进行扩展

### ✅ 项目实施
- **水平扩展**: 运行多个应用实例（通过 Nginx 负载均衡）
- **进程类型**:
  - Web 进程: 处理 HTTP 请求
  - Worker 进程: 异步邮件发送（使用 `@Async`）

### 🔍 检查点
- [ ] 应用无状态，支持水平扩展
- [ ] 后台任务通过异步方法或消息队列处理
- [ ] 不依赖线程内存共享

### ✅ 正确做法
```java
// ✅ 异步处理耗时任务
@Service
public class EmailService {
    @Async
    public CompletableFuture<Void> sendVerificationEmailAsync(String email) {
        // 异步发送邮件，不阻塞主请求
    }
}

// ✅ 配置线程池
@Configuration
@EnableAsync
public class AsyncConfig implements AsyncConfigurer {
    @Override
    public Executor getAsyncExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(5);
        executor.setMaxPoolSize(10);
        return executor;
    }
}
```

---

## IX. Disposability (易处理)
**原则**: 快速启动和优雅终止

### ✅ 项目实施
- **快速启动**: Spring Boot 应用启动时间 < 30 秒
- **优雅关闭**: 接收到 SIGTERM 后完成当前请求再退出

### 🔍 检查点
- [ ] 应用启动时间合理（避免过度初始化）
- [ ] 支持优雅关闭（`spring.lifecycle.timeout-per-shutdown-phase`）
- [ ] 数据库连接正确关闭

### ✅ 配置
```yaml
# application.yml
spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s  # 优雅关闭超时
```

```java
// ✅ 使用 @PreDestroy 清理资源
@Component
public class ResourceManager {
    @PreDestroy
    public void cleanup() {
        // 关闭数据库连接、清理缓存等
    }
}
```

---

## X. Dev/Prod Parity (开发环境与线上环境等价)
**原则**: 尽可能保持开发、预发布、线上环境相同

### ✅ 项目实施
- **相同数据库**: 开发和生产都使用 PostgreSQL（不要开发用 H2，生产用 PostgreSQL）
- **相同构建**: 同一个 JAR 部署到所有环境
- **相同配置结构**: 只有环境变量值不同

### 🔍 检查点
- [ ] 开发和生产使用相同数据库类型
- [ ] 开发和生产使用相同的依赖版本
- [ ] 避免"仅在开发环境"的代码路径

### ❌ 禁止做法
```java
// ❌ 不要在不同环境使用不同的后端服务
@Profile("dev")
@Configuration
public class DevDatabaseConfig {
    // 使用 H2 内存数据库
}

@Profile("prod")
@Configuration
public class ProdDatabaseConfig {
    // 使用 PostgreSQL
}
```

### ✅ 正确做法
- 所有环境都使用 PostgreSQL
- 通过环境变量配置不同的数据库地址
- 开发环境使用本地 PostgreSQL 容器（Docker）

---

## XI. Logs (日志)
**原则**: 把日志当作事件流

### ✅ 项目实施
- **输出到 stdout/stderr**: 应用不管理日志文件
- **集中收集**: 使用 systemd journal 或日志聚合工具
- **结构化日志**: 使用 Logback JSON 格式（可选）

### 🔍 检查点
- [ ] 应用输出日志到 stdout（不直接写文件）
- [ ] 日志格式统一（时间戳、级别、消息）
- [ ] 不在应用中配置日志轮转（由运维工具处理）

### ✅ 正确做法
```xml
<!-- logback-spring.xml -->
<configuration>
    <!-- ✅ 开发环境：输出到控制台 -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ✅ 生产环境：输出到控制台，由 systemd 收集 -->
    <springProfile name="prod">
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{ISO8601} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>
</configuration>
```

### ❌ 禁止做法
```java
// ❌ 不要在应用中管理日志文件
FileAppender appender = new FileAppender();
appender.setFile("/var/log/myapp/app.log");  // 不推荐

// ❌ 不要使用 System.out.println
System.out.println("User logged in");  // 使用日志框架

// ✅ 应该使用 SLF4J
log.info("User logged in: {}", username);
```

---

## XII. Admin Processes (管理进程)
**原则**: 后台管理任务作为一次性进程运行

### ✅ 项目实施
- **数据库迁移**: Flyway 作为一次性任务运行
- **数据修复**: 编写独立脚本，不在主应用中
- **定时任务**: 使用 `@Scheduled` 或独立的 Cron 任务

### 🔍 检查点
- [ ] 数据库迁移通过 Flyway 独立管理
- [ ] 管理任务使用相同的代码库
- [ ] 管理任务在相同的环境中运行

### ✅ 正确做法
```java
// ✅ 使用 Flyway 管理数据库迁移
// src/main/resources/db/migration/V1__create_users_table.sql

// ✅ 定时任务
@Component
public class ScheduledTasks {
    @Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨 2 点
    public void cleanupExpiredTokens() {
        refreshTokenRepository.deleteExpiredTokens();
    }
}

// ✅ 一次性管理脚本
public class DataMigrationScript {
    public static void main(String[] args) {
        // 运行一次性数据迁移
        // java -cp app.jar com.seaworld.DataMigrationScript
    }
}
```

---

## 🔍 12-Factor 合规性检查清单

在开发新功能或审查代码时，使用此清单确保符合 12-Factor 原则：

### 配置管理
- [ ] 所有环境相关配置使用环境变量
- [ ] 敏感信息不在代码或 yml 中硬编码
- [ ] `.env` 文件在 `.gitignore` 中
- [ ] `application-{profile}.yml` 只包含占位符

### 依赖管理
- [ ] 所有依赖在 `pom.xml` / `package.json` 中声明
- [ ] 依赖版本精确指定
- [ ] 不依赖系统级工具

### 构建和部署
- [ ] 同一个构建产物可部署到任何环境
- [ ] 发布版本有唯一标识
- [ ] 不在生产环境编译代码

### 应用架构
- [ ] 应用无状态（可水平扩展）
- [ ] 不使用服务器端 Session
- [ ] 后端服务通过 URL 配置
- [ ] 日志输出到 stdout

### 运维友好
- [ ] 支持优雅关闭
- [ ] 快速启动（< 30 秒）
- [ ] 健康检查端点可用

---

## 🛠️ Copilot 集成规范

当 Copilot 生成代码或提供建议时，应自动遵循以下规范：

### 生成配置相关代码
```java
// ✅ Copilot 应该生成
@Value("${app.feature.timeout}")
private int timeout;

// ❌ Copilot 不应该生成
private int timeout = 5000;  // 硬编码
```

### 生成后端服务连接代码
```java
// ✅ Copilot 应该生成
@Value("${redis.url}")
private String redisUrl;

// ❌ Copilot 不应该生成
private String redisUrl = "redis://localhost:6379";
```

### 提示配置问题
当检测到以下模式时，Copilot 应提示违反 12-Factor 原则：
- 硬编码的密码、密钥、URL
- 在代码中判断环境（`if env == "prod"`）
- 使用 HttpSession 存储状态
- 直接写文件日志

---

## 📚 参考资源

- [The Twelve-Factor App](https://12factor.net/) - 官方文档
- [The Twelve-Factor App (中文版)](https://12factor.net/zh_cn/) - 中文翻译
- [Spring Boot Production Best Practices](https://docs.spring.io/spring-boot/docs/current/reference/html/deployment.html)

---

**最后更新**: 2026-04-18  
**版本**: 1.0
