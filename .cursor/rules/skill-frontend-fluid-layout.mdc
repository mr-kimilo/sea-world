---
description: "前端流式布局速查 (Fluid layout cheat sheet)"
globs: frontend/**/*
alwaysApply: false
---

> **Source of truth**: `.github/skills/frontend-fluid-layout.md` (edit there, then re-run this script.)

# 前端流式布局速查 (Fluid layout cheat sheet)

面向 **Cursor / Copilot / 人工** 的快速对齐，完整条文见 [.github/instructions/frontend-fluid-layout.instructions.md](../instructions/frontend-fluid-layout.instructions.md)。

| 要点 | 做法 |
|------|------|
| 断点 | 视口 `< 1024px` = mobile，`≥ 1024` = web；与 `useDeviceType.ts` 一致 |
| 页面骨架 | 主内容 `flex: 1; min-height: 0; overflow: auto`；头尾 `flex-shrink: 0` |
| 宽度 | `box-sizing: border-box`；少用撑破小屏的固定 `px` 总宽 |
| Flex 子项 | 横向易溢出时 `min-width: 0` |
| 按钮行 | `flex-wrap` + `gap`；窄屏可纵向堆叠 |
| Grid | `minmax(0, 1fr)` 或 `minmax(min(100%, …), 1fr)` |
| 验证 | 320px ↔ 宽屏拖动；登录/首页/商城/个人中心走一遍 |

产品 UI 说明：`docs/ui/ui.md`。重构提示词：`docs/ui/ui-v2.md`。
