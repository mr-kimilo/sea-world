---
description: "Dual Token Authentication Pattern (双 Token 认证设计)"
alwaysApply: true
---

> **Source of truth**: `.github/skills/auth-pattern.md` (edit there, then re-run this script.)

# Dual Token Authentication Pattern (双 Token 认证设计)

- **Access Token**: 短效 (30min)，携带角色权限 (Claims)，拦截器无状态校验，保证访问效率。
- **Refresh Token**: 长效 (7days)，数据库持久化，仅存用户 ID。
- **Mechanism (机制)**: Access 过期时前端拦截并用 Refresh 换取新的一对 Token (自动轮换)，实现用户无感续期，兼顾安全与性能。
