---
description: "Use when editing frontend layout, CSS, or responsive behavior: pages, layouts, headers, tab bars, forms, buttons, cards, grids, or any UI that must adapt to viewport width."
globs: frontend/**/*
alwaysApply: false
---

# 前端流式布局规范 (Fluid Layout)

本规范适用于 **海底世界** 前端（`frontend/`）。目标：在手机与 Web 上均以 **视口宽度** 流式排布，避免按钮越界、整页被裁切、意外横向滚动等问题。  
详细产品说明见 `docs/ui/ui.md`；重构背景与 AI 提示词见 `docs/ui/ui-v2.md`。

## 1. 断点与设备语义（必须一致）

- **唯一断点**：视口宽度 **`< 1024px` 为移动端（mobile）**，**`≥ 1024px` 为 Web**。与 `frontend/src/hooks/useDeviceType.ts` 中 `MOBILE_BREAKPOINT = 1024` **保持一致**，不得另立「手机型号」或 User-Agent 判断宽窄布局。
- **运行时标记**：`useDeviceType` 在 `<html>` 上设置 `data-device="mobile"` 或 `data-device="web"`。样式可在 `*.css` / `*.mobile.css` 中继续用 `[data-device="mobile"]` 区分信息架构（如底栏 Tab、表格改卡片），但 **mobile 与 web 都必须流式**，不能假设固定总宽度（如仅大屏可用）。
- **CSS 媒体查询**：与断点相关的 `max-width` / `min-width` 应以 **1023px / 1024px** 与上述逻辑对齐（注意 `max-width: 1024px` 与 `width < 1024` 的 off-by-one，以 JS 钩子为准时，媒体查询需与现有文件保持一致）。

## 2. 整体页面结构（Page shell）

- **根区域**：`#root` 使用 `box-sizing: border-box`；宽度应为 `100%` 配合 `max-width`（若有），且 **padding/border 计入宽度**，小屏下不溢出视口。
- **垂直填满**：页面级容器优先使用 **`min-height: 100dvh`** 或 **`100svh`**（需兼容时可用 `min-height: 100vh` 作回退），避免内容「视觉截断」；注意移动端动态工具栏，关键内容不要紧贴视口底缘而无滚动余地。
- **经典三区**：
  - **顶栏**：可为 `flex-shrink: 0`。
  - **主内容**：`flex: 1; min-height: 0; overflow: auto`（或等价），保证 **内部列表再长也能滚动**。
  - **底栏 / `MobileTabBar`**：`flex-shrink: 0`；主内容区预留底部空间或使用布局避免内容被遮挡。
- **禁止**：仅靠 `overflow-x: hidden`「盖住」布局错误而不修根因；除非明确只做兜底且已消除主要溢出源。

## 3. 流式尺寸与单位

- **优先**：`%`、`fr`、`min()`、`max()`、`clamp()`、flex/grid；栅格用 **`minmax(0, 1fr)`** 避免子项撑破轨道。
- **慎用**：大段横向布局上的 **固定 `px` 宽度**；若使用，需保证在最小目标宽度（如 **320px**）下仍不溢出或可换行。
- **表单与按钮**：输入框、主按钮在容器内 **`width: 100%` / `max-width: 100%`** 与父级约束一致；按钮组 **允许 `flex-wrap`**，窄屏时 **纵向堆叠** 优于挤出视口。

## 4. 防溢出（Flex / Grid 必备）

- 容器统一 **`box-sizing: border-box`**。
- **Flex 子项** 在横向收缩场景下设置 **`min-width: 0`**（必要时配合 `overflow: hidden`、`text-overflow: ellipsis`），避免子项内容把兄弟节点或整行挤出视口。
- **长文案**：使用 **`overflow-wrap: anywhere`** 或 **`word-break`**（按语言与产品设计选择），避免未断行字符串撑出横向滚动。
- **图片/媒体**：`max-width: 100%; height: auto`（或约束在卡片内）。

## 5. 简单组件级约定

- **工具栏 / 按钮行**：`display: flex; flex-wrap: wrap; gap: …`；主次按钮在极窄宽度下可 **单列**。
- **卡片列表**：Grid 使用 `repeat(auto-fill, minmax(min(100%, …), 1fr))` 等模式时，**`min` 内必须含 `100%` 或等价**，避免列宽小于视口时溢出。
- **表格（Web）**：若表格宽于视口，应在 **独立滚动容器** 内横向滚动（`overflow-x: auto`），而不是整页横向滚动；移动端优先按产品说明改为 **卡片列表**。
- **固定定位**：`position: fixed` 的栏需考虑 **`env(safe-area-inset-*)`**，避免与刘海/手势条重叠。

## 6. 与现有代码的协作

- 页面/组件继续 **组件名 + 同名 `.css`**；移动端增量样式可保留 **`*.mobile.css`** 或 `[data-device="mobile"]` 选择器，但须遵守本节流式规则。
- **国际化**：用户可见文案仍遵守 [frontend-i18n.instructions.md](frontend-i18n.instructions.md)，布局规范不替代 i18n 规范。
- **对话框**：仍遵守 [frontend-dialog.instructions.md](frontend-dialog.instructions.md)；弹层在大屏与小屏下均应 **限制 `max-width`** 且 **内部可滚动**。

## 7. 自检清单（改完即检）

- [ ] 浏览器窗口从 **320px 拖到 ≥1024px** 及反向，主要页面无 **非预期的** 横向滚动条。
- [ ] **登录 / 注册 / 验证邮箱 / 首页 / 商城 / 个人中心** 主路径可操作，主按钮不被裁切。
- [ ] 有底栏时，最后一项内容 **可滚动至完全可见**，不被遮挡。
- [ ] 未改动 `MOBILE_BREAKPOINT` 语义；新增媒体查询与 **1024** 策略一致、无矛盾。

## 8. 反模式（避免）

- 仅用「这是手机 CSS」写死宽版布局，Web 端仍用 **过宽固定像素** 导致小窗口异常。
- 多层嵌套 `width: 100vw` 且同时有竖向滚动条，引发 **横向抖动**（100vw 含滚动条宽度）。
- 在 flex 子项内放 **不可收缩的长内容** 却不设 `min-width: 0` 或截断策略。
