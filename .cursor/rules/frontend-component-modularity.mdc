---
description: "React modularity: single responsibility, container vs presentational."
globs: frontend/**/*
alwaysApply: false
---

# React Component Modularity Guidelines

## Overview

组件应该保持简洁、专注，遵循单一职责原则。当一个组件变得过于复杂时，应该拆分成更小的子组件。

## When to Split Components

当组件出现以下情况时，应该考虑拆分：

1. **代码行数过多**：单个组件超过 200 行代码
2. **功能过多**：组件处理多个独立的业务逻辑
3. **状态过多**：组件内部管理超过 5 个独立状态
4. **可复用性**：某部分逻辑可以在其他地方复用

## Component Structure

推荐的组件目录结构：

```
pages/
  PageName/
    PageName.tsx          # 主页面组件（容器组件）
    PageName.css          # 页面样式
    components/           # 页面专属子组件
      SubComponent1.tsx
      SubComponent2.tsx
      SubComponent3.tsx
```

## Modularity Principles

### 1. 单一职责原则 (Single Responsibility)

每个组件只负责一个功能模块：

- ✅ **正确**：`ItemList.tsx` 只负责展示商品列表和购买操作
- ❌ **错误**：`Shop.tsx` 同时处理商品列表、订单管理、商品创建等多个功能

### 2. 容器组件 vs 展示组件

**容器组件（Container Component）**：
- 负责状态管理和业务逻辑
- 协调子组件之间的通信
- 处理路由和导航
- 示例：`Shop.tsx`

**展示组件（Presentational Component）**：
- 专注于 UI 展示
- 通过 props 接收数据和回调
- 可独立测试和复用
- 示例：`ItemList.tsx`, `PendingOrders.tsx`

### 3. 组件通信

使用 **props** 和 **回调函数** 进行父子组件通信：

```tsx
// 父组件传递数据和回调
<ItemList 
  selectedChild={selectedChild} 
  onOrderCreated={handleDataChanged}
/>

// 子组件接收并使用
interface ItemListProps {
  selectedChild: Child | null;
  onOrderCreated?: () => void;
}

export default function ItemList({ selectedChild, onOrderCreated }: ItemListProps) {
  // 操作完成后通知父组件
  onOrderCreated?.();
}
```

### 4. 状态管理

- **本地状态**：组件内部使用 `useState` 管理自己的数据（如 loading、items）
- **共享状态**：使用 Zustand store 管理跨组件的全局状态（如 selectedChild）
- **父组件状态**：通过 props 传递给子组件

### 5. 数据获取

每个子组件负责获取自己的数据：

```tsx
// ✅ 正确：子组件自己管理数据获取
export default function ItemList({ selectedChild }: ItemListProps) {
  const [items, setItems] = useState<ShopItem[]>([]);
  
  useEffect(() => {
    loadItems();
  }, [selectedChild]);
  
  const loadItems = async () => {
    const data = await getAvailableItems(selectedChild.id);
    setItems(data);
  };
}

// ❌ 错误：父组件获取所有数据并传递
// 这会导致父组件过于臃肿
```

## Real-World Example: Shop Page Refactoring

### Before (臃肿的单文件)

```tsx
// Shop.tsx - 350+ lines
export default function Shop() {
  // 商品相关状态
  const [items, setItems] = useState([]);
  const [loadingItems, setLoadingItems] = useState(false);
  
  // 订单相关状态
  const [pendingOrders, setPendingOrders] = useState([]);
  const [completedOrders, setCompletedOrders] = useState([]);
  const [loadingOrders, setLoadingOrders] = useState(false);
  
  // 商品相关逻辑
  const loadItems = async () => { /* ... */ };
  const handleBuy = async () => { /* ... */ };
  
  // 订单相关逻辑
  const loadOrders = async () => { /* ... */ };
  const handleConfirm = async () => { /* ... */ };
  const handleCancel = async () => { /* ... */ };
  
  // 大量 JSX 渲染逻辑
  return (
    <div>
      {/* 200+ lines of JSX */}
    </div>
  );
}
```

### After (模块化拆分)

```tsx
// Shop.tsx - 100 lines (主容器)
export default function Shop() {
  const [activeTab, setActiveTab] = useState('items');
  const [refreshKey, setRefreshKey] = useState(0);
  
  const handleDataChanged = () => {
    setRefreshKey(prev => prev + 1);
  };
  
  return (
    <div className="shop-page">
      <ShopHeader />
      <ShopTabs activeTab={activeTab} onTabChange={setActiveTab} />
      <ShopContent>
        {activeTab === 'items' && (
          <ItemList 
            selectedChild={selectedChild}
            onOrderCreated={handleDataChanged}
          />
        )}
        {activeTab === 'pending' && (
          <PendingOrders 
            selectedChild={selectedChild}
            onOrderChanged={handleDataChanged}
          />
        )}
        {activeTab === 'completed' && (
          <CompletedOrders selectedChild={selectedChild} />
        )}
      </ShopContent>
    </div>
  );
}

// components/ItemList.tsx - 120 lines
export default function ItemList({ selectedChild, onOrderCreated }: ItemListProps) {
  const [items, setItems] = useState([]);
  const [loading, setLoading] = useState(false);
  
  const loadItems = async () => { /* ... */ };
  const handleBuy = async () => { 
    /* ... */
    onOrderCreated?.();
  };
  
  return <div className="items-grid">{/* ... */}</div>;
}

// components/PendingOrders.tsx - 140 lines
export default function PendingOrders({ selectedChild, onOrderChanged }: PendingOrdersProps) {
  const [orders, setOrders] = useState([]);
  const [loading, setLoading] = useState(false);
  
  const loadOrders = async () => { /* ... */ };
  const handleConfirm = async () => { 
    /* ... */
    onOrderChanged?.();
  };
  const handleCancel = async () => { 
    /* ... */
    onOrderChanged?.();
  };
  
  return <div className="orders-list">{/* ... */}</div>;
}

// components/CompletedOrders.tsx - 80 lines
export default function CompletedOrders({ selectedChild }: CompletedOrdersProps) {
  const [orders, setOrders] = useState([]);
  const [loading, setLoading] = useState(false);
  
  const loadOrders = async () => { /* ... */ };
  
  return <div className="orders-list">{/* ... */}</div>;
}
```

## Benefits of Modular Components

1. **可读性**：每个文件职责清晰，易于理解
2. **可维护性**：修改某个功能只需关注对应的组件
3. **可测试性**：子组件可以独立测试
4. **可复用性**：组件可以在其他地方复用
5. **性能优化**：更容易应用 React.memo 等优化手段
6. **团队协作**：多人可以并行开发不同组件

## Common Patterns

### Pattern 1: List with Actions

```tsx
// ListComponent.tsx
interface ListComponentProps {
  items: Item[];
  onItemAction: (item: Item) => void;
}

export default function ListComponent({ items, onItemAction }: ListComponentProps) {
  return (
    <div className="list">
      {items.map(item => (
        <ItemCard key={item.id} item={item} onAction={onItemAction} />
      ))}
    </div>
  );
}
```

### Pattern 2: Form Component

```tsx
// FormComponent.tsx
interface FormComponentProps {
  initialData?: FormData;
  onSubmit: (data: FormData) => Promise<void>;
  onCancel?: () => void;
}

export default function FormComponent({ initialData, onSubmit, onCancel }: FormComponentProps) {
  const [formData, setFormData] = useState(initialData || {});
  const [submitting, setSubmitting] = useState(false);
  
  const handleSubmit = async () => {
    setSubmitting(true);
    await onSubmit(formData);
    setSubmitting(false);
  };
  
  return <form>{/* ... */}</form>;
}
```

### Pattern 3: Tab Container

```tsx
// TabContainer.tsx
type TabType = 'tab1' | 'tab2' | 'tab3';

export default function TabContainer() {
  const [activeTab, setActiveTab] = useState<TabType>('tab1');
  
  return (
    <div>
      <TabButtons activeTab={activeTab} onTabChange={setActiveTab} />
      <TabContent activeTab={activeTab} />
    </div>
  );
}
```

## Best Practices Checklist

When creating or refactoring components, ask yourself:

- [ ] 组件是否只负责一个功能？
- [ ] 组件代码行数是否少于 200 行？
- [ ] 组件的 props 是否少于 10 个？
- [ ] 组件是否可以独立测试？
- [ ] 组件的命名是否清晰表达其职责？
- [ ] 组件是否可以在其他地方复用？
- [ ] 组件的状态管理是否合理？
- [ ] 组件是否有清晰的接口（props 类型定义）？

## Anti-Patterns to Avoid

### ❌ God Component（上帝组件）

一个组件做所有事情，超过 500 行代码。

### ❌ Prop Drilling（Props 穿透）

通过多层组件传递 props。应该使用 Context 或状态管理库。

### ❌ Duplicate Logic（重复逻辑）

多个组件有相同的逻辑。应该提取到自定义 Hook 或工具函数。

### ❌ Tight Coupling（紧耦合）

子组件直接访问父组件的状态。应该通过 props 和回调通信。

## Conclusion

保持组件模块化是 React 应用质量的关键。遵循这些原则，你的代码将更加：

- 🎯 **清晰**：每个组件职责明确
- 🔧 **易维护**：修改影响范围小
- 🧪 **易测试**：组件可独立测试
- 🚀 **高性能**：更好的优化空间
- 👥 **协作友好**：团队开发更顺畅
